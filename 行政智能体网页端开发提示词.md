# **角色设定**

你是一个资深的全栈开发工程师和企业产品架构师。现在请帮我从零开始开发一个名为“行政智能体 (Admin Agent)”的内部网页端系统。

# **1\. 项目概述**

这是一个面向企业行政后勤团队的智能化协同与资产管理系统。核心目标是：

1. **智能客服拦截：** 员工咨询行政SOP（如WiFi密码、报销制度）时，大模型直接解答。  
2. **非结构化转工单：** 接收员工的复杂诉求（文本/语音/图片），通过大模型自动解析为带有结构化字段的工单（采购/报修/预定等）。  
3. **资产与审批联动：** 自动匹配公司资产库，并根据规则触发跨部门审批流（业务主管/财务）。  
4. **全局看板：** 提供行政效能、成本支出与资产健康度的数据看板。

# **2\. 技术栈要求**

* **后端核心:** **必须使用 FastAPI 框架 (Python)** 构建后端 API。  
* **前端:** 请推荐合适的前端框架（例如 Vue3, React 或 Next.js），要求界面现代化、B端商务风，支持良好的表单和看板交互。  
* **数据库:** 请推荐合适的数据库（例如 PostgreSQL 或 MySQL），并使用适合 FastAPI 的 ORM 工具（例如 SQLAlchemy 或 SQLModel）。可选建议：引入轻量级向量库（如 Chroma/FAISS）用于行政SOP问答。  
* **AI 接口集成:** **必须基于标准 OpenAI 协议规范**实现 AI Service 层代码（以便无缝兼容 ChatGPT、DeepSeek、阿里云千问等主流大模型）。系统需支持在前端“设置”页面动态配置大模型参数（Base URL、API Key、Model Name），并需支持多模态（文本与图片识别）能力的调用。

# **3\. 核心数据库模型设计 (Database Schema)**

请在初始化项目时，基于你选择的 ORM 工具设计以下核心表结构：

1. **User (用户表):** ID, 姓名, 密码哈希, 角色(员工/行政/主管/系统管理员), 所属部门, 创建时间。  
2. **Asset (资产库表):** ID, 资产编号, 资产名称, 分类(IT设备/办公家具/耗材), 状态(闲置/使用中/维修中/报废), 归属人ID, 当前库存量。  
3. **Ticket (行政工单表):** ID, 发起人ID, 原始文本, 附件图片URL, 工单类型(采购/报修/申领/咨询), 关联资产ID, 预估费用, 审批状态(无需审批/待主管审批/待财务审批/已通过), 处理状态(待接单/处理中/已闭环), 创建/更新时间。  
4. **Task (行政执行任务表):** ID, 关联工单ID, 任务名称, 供应商/外部对接人, 负责人ID(行政人员), 截止时间, 状态。  
5. **KnowledgeBase (知识库SOP表):** ID, 标题, 内容详情, 向量化特征(可选), 适用场景。  
6. **SysConfig (系统配置表):** ID, 配置键名(如 LLM\_API\_KEY, LLM\_BASE\_URL, LLM\_MODEL), 配置键值, 描述, 更新时间。（用于让系统管理员在前端界面动态配置和切换大模型）

# **4\. 核心功能模块与开发步骤**

请严格分阶段为我输出代码或执行指令：

## **第一阶段：项目初始化与系统基座 (权限与配置)**

* 搭建 FastAPI 后端项目基础目录结构。  
* 配置数据库连接与 ORM 模型。  
* 实现基于 JWT 的登录/注册系统，以及基于 RBAC 的权限中间件（员工看自己、主管看本部门、行政看全局）。  
* **系统设置中心:** 实现 SysConfig 表的 CRUD 接口。后续大模型调用必须实时从该表读取 Base URL 和 API Key，不将密钥硬编码在后端。

## **第二阶段：多模态输入与 AI 意图路由 (核心入口)**

* **前端端:** 开发一个类似聊天框的输入界面，支持文字输入和图片上传。提供一个“系统设置”弹窗供管理员配置 OpenAI 协议的大模型接口。  
* **FastAPI Service 层 (AI 路由):** 编写基于 OpenAI SDK（Python版）的大模型调用逻辑，实现意图分发：  
  * *分支 A (知识问答):* 判断为咨询类，从 KnowledgeBase 检索答案直接回复，不建单。  
  * *分支 B (工单生成):* 判断为动作类，提取结构化 JSON（类型、物品、紧急度），存入 Ticket 表。若涉及费用或贵重物品，自动将 审批状态 设为“待审批”。

## **第三阶段：资产联动与行政排班看板 (核心处理)**

* **前端端:**  
  * 开发“审批中心”（供主管/财务点击通过/驳回）。  
  * 开发行政专员的“任务看板”（Kanban拖拽视图）。  
* **FastAPI 接口:**  
  * 编写从 Ticket 和 Task 表拉取数据的接口。  
  * 实现工单状态流转接口：审批通过后自动触发库存扣减（修改 Asset 表）或生成采购 Task。  
  * 行政人员可更新 Task 进度、填写实际花费。

## **第四阶段：管理层数据洞察 Dashboard**

* **前端端:** 使用图表库（如 ECharts/Chart.js）开发大盘数据。  
* **FastAPI 接口:** 提供聚合数据，包括：  
  1. 当月行政总支出与各部门成本分摊。  
  2. 各类资产的损耗率/报修频率（找出劣质供应商）。  
  3. 行政团队响应 SLA（平均接单与闭环时间）。

# **5\. 代码规范与输出要求**

* 所有的关键业务逻辑必须包含中文注释。  
* 请按模块化开发，合理划分 FastAPI 的 Routers (如 users, configs, tickets, assets, ai\_agent)。  
* 考虑到实际开发，请先向我确认上述数据库结构和你推荐的前端/数据库技术栈是否合理。确认后，请**从“第一阶段”的初始化命令和 FastAPI 基础目录结构开始**输出代码。